/*

const accountState = {
  accountId: ""
}

const accountInfo(state = accountState, action) {
  switch(action.type) {
    case 'LOGIN_SUCCESS' :
          return {
            ...state,
            idToken: action.idToken
    }

export default accountInfo;

*/
